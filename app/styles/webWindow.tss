"#webWindow": {
	modal: true,
	navBarHidden: false,	// Modal windows on iOS no longer have a navigation controller and hence do not have a nav bar.
	layout: "vertical",
	backgroundColor: "white"
},
// override app.tss
"#titleLabel": {
	top: 34,
	// left: 50,
	right: 0,
	width: Ti.UI.SIZE,
	font: {
		fontSize: 16
	}
}

"#webView": {
	disableBounce: true	// only iOS
}

"#closeButton": {
	// borderWidth : 1,
	// borderRadius : 3,
	// borderColor : 'red',
}
